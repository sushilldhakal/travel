{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "installCommand": "npm install --force",
    "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}