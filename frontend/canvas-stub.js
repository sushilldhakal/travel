// Stub for canvas module to prevent build errors
// This is used by konva which is a dependency of react-filerobot-image-editor
// Canvas is only needed for server-side rendering, which we don't use for the image editor

module.exports = {};
