/**
 * Settings Route Page
 * 
 * Renders the settings page for API integrations.
 * Authentication is handled by the dashboard layout.
 */
'use client';

import { SettingsPage } from '@/components/dashboard/settings/SettingsPage';

export default function SettingsRoute() {
    return <SettingsPage />;
}
